---
name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # ─── Path Filtering ─────────────────────────────────────────────────
  changes:
    name: Detect Changed Components
    runs-on: ubuntu-latest
    outputs:
      server: ${{ steps.filter.outputs.server }}
      sdk-python: ${{ steps.filter.outputs.sdk-python }}
      sdk-typescript: ${{ steps.filter.outputs.sdk-typescript }}
      website: ${{ steps.filter.outputs.website }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            server:
              - 'server/**'
              - 'tests/**'
              - '.github/workflows/ci.yml'
            sdk-python:
              - 'sdks/python/**'
              - '.github/workflows/ci.yml'
            sdk-typescript:
              - 'sdks/typescript/**'
              - '.github/workflows/ci.yml'
            website:
              - 'website/**'
              - '.github/workflows/ci.yml'

  # ─── Lint & Format (Server) ─────────────────────────────────────────
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.server == 'true' }}
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Ruff lint (production)
        run: uv run ruff check kyros/ alembic/

      - name: Ruff lint (tests — relaxed)
        run: uv run ruff check tests/ --select E,F,W --ignore E501

      - name: Ruff format check
        run: uv run ruff format --check .

  # ─── Type Checking (Server) ─────────────────────────────────────────
  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.server == 'true' }}
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Mypy
        run: uv run mypy kyros/

  # ─── Unit Tests (Server) ────────────────────────────────────────────
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.server == 'true' }}
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Run unit tests
        run: uv run pytest tests/unit/ -v --tb=short -q

  # ─── Edge Tests (Server) ────────────────────────────────────────────
  edge-tests:
    name: Edge Tests
    runs-on: ubuntu-latest
    needs: [changes, lint, typecheck]
    if: ${{ needs.changes.outputs.server == 'true' }}
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Run edge tests
        run: uv run pytest tests/edge/ -v --tb=short -q

  # ─── Integration Tests (Postgres + Redis) ──────────────────────────
  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: [changes, unit-tests, edge-tests]
    if: ${{ needs.changes.outputs.server == 'true' }}
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: kyros_test
          POSTGRES_USER: kyros
          POSTGRES_PASSWORD: test
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U kyros -d kyros_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports: ["6379:6379"]
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    defaults:
      run:
        working-directory: server

    env:
      KYROS_DATABASE_URL: postgresql+asyncpg://kyros:test@localhost:5432/kyros_test
      KYROS_REDIS_URL: redis://localhost:6379/0
      KYROS_JWT_SECRET_KEY: ci-test-secret-key-minimum-32-chars!!
      KYROS_ENVIRONMENT: test

    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Run migrations
        run: uv run alembic upgrade head

      - name: Seed test data
        run: uv run python tests/seed_test_tenant.py

      - name: Start server
        run: |
          uv run uvicorn kyros.main:app --host 0.0.0.0 --port 8000 > server.log 2>&1 &
          echo $! > server.pid
          for i in {1..30}; do
            curl -sf http://localhost:8000/health/ready && echo " ✅ Server ready" && exit 0
            sleep 1
          done
          echo "❌ Server failed to start" && cat server.log && exit 1

      - name: Integration tests
        run: >-
          uv run pytest tests/integration/ -v --tb=short -m "not slow"
          --ignore=tests/integration/test_proxy.py

      - name: Upload server logs
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: server-logs
          path: server/server.log

  # ─── Python SDK ────────────────────────────────────────────────────
  sdk-python:
    name: Python SDK
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.sdk-python == 'true' }}
    defaults:
      run:
        working-directory: sdks/python
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Lint
        run: uv run ruff check kyros/

      - name: Type check
        run: uv run mypy kyros/

  # ─── TypeScript SDK ────────────────────────────────────────────────
  sdk-typescript:
    name: TypeScript SDK
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.sdk-typescript == 'true' }}
    defaults:
      run:
        working-directory: sdks/typescript
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx tsc --noEmit

      - name: Lint
        run: npm run lint

  # ─── Next.js Website ────────────────────────────────────────────────
  website:
    name: Website Build & Lint
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.website == 'true' }}
    defaults:
      run:
        working-directory: website
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: website/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

  # ─── CI Summary Gate ──────────────────────────────────────────────
  ci-pass:
    name: CI ✅
    if: always()
    needs: [changes, lint, typecheck, unit-tests, edge-tests, integration-tests, sdk-python, sdk-typescript, website]
    runs-on: ubuntu-latest
    steps:
      - name: Check all jobs
        run: |
          echo "Lint: ${{ needs.lint.result }}"
          echo "Typecheck: ${{ needs.typecheck.result }}"
          echo "Unit tests: ${{ needs.unit-tests.result }}"
          echo "Edge tests: ${{ needs.edge-tests.result }}"
          echo "Integration: ${{ needs.integration-tests.result }}"
          echo "SDK Python: ${{ needs.sdk-python.result }}"
          echo "SDK TS: ${{ needs.sdk-typescript.result }}"
          echo "Website: ${{ needs.website.result }}"

          if [[ "${{ needs.lint.result }}" == "failure" ]] || \
             [[ "${{ needs.typecheck.result }}" == "failure" ]] || \
             [[ "${{ needs.unit-tests.result }}" == "failure" ]] || \
             [[ "${{ needs.edge-tests.result }}" == "failure" ]] || \
             [[ "${{ needs.integration-tests.result }}" == "failure" ]] || \
             [[ "${{ needs.sdk-python.result }}" == "failure" ]] || \
             [[ "${{ needs.sdk-typescript.result }}" == "failure" ]] || \
             [[ "${{ needs.website.result }}" == "failure" ]]; then
            echo "❌ CI failed"
            exit 1
          fi
          echo "✅ All required checks passed"
